# Dépendances
node_modules/

# Données runtime (registre, clés, certificats, config, dossiers) — ne JAMAIS versionner les secrets
data/keys/
data/tls/
data/*.jsonl
data/*.json
data/*.bak
data/*.lock
data/*.ckpt
data-backup/
data/keys/*.key
data-demo/

# Secrets & environnement
.env
.env.*
*.pem
*.key
*.crt

# Espaces de travail d'agents (sessions Claude parallèles)
.claude/worktrees/
.omc/

# Fichiers verrous Office (document ouvert)
~$*

# Système
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
